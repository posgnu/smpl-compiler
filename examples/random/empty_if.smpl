main
var a, b;
{
	let a <- 1;
	let b <- 2;

	if a < b
	then
		let a <- 3;
	else
		let b <- 4;
	fi;

	if a < b
	then
		let a <- 5;
	else
		let b <- 1;
	fi;

	if a < b
	then
		let a <- 0;
	fi;

	call OutputNum(a);
	call OutputNum(b);
}.