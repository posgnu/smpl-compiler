main var a,b,c,d,e,g,f, aaaa; {
	let a <- 2;
	let b<-a+2;
	let c<-a+b;
	let d<-(a+b)*4+c;
	if a > 0 then
		if b > 0 then
			if c > 0 then
			let e<-20;
			fi;
		fi;
	else 
		let e<-c+d;
	fi; 
	let g<-e+a; }.

