main
var a, b;
void function test (x, y);
var z;
{
  let z <- x + y;
  call OutputNum(z);
};
{
	let a <- call InputNum;

	let b <- a + 1;

	while a + 1 != b
	do
		let a <- a + 1;
	od;

    call test(a+b, a*b);
	call OutputNum(a);
}.